policies:
  - name: dynamodb-pitr-enabled
    description: DynamoDB tables must have point-in-time recovery enabled
    resource: terraform.aws_dynamodb_table
    filters:
      - not:
        - type: value
          key: "point_in_time_recovery.enabled"
          value: true
